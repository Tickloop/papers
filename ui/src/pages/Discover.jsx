import React from 'react';

import { PaperQueue } from '../components/PaperQueue';

const Discover = () => {
    return (
        <div>
            <PaperQueue/>
        </div>

    );
};

export default Discover;
